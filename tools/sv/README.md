# Конвейер сборки /ksh/sv/ — Śivastotrāvalī

Страницы раздела **не пишутся руками**: они собираются из одной страницы
источника — [узла 1005](https://www.sanskrit-trikashaivism.com/en/scriptures-trika-scriptures-shivastotraavalii/1005)
на sanskrit-trikashaivism.com.

Главное, что нужно знать об этом писании: **опереться почти не на что.**
Санскрит выложен целиком — 450 строф, деванагари и транслитерация, — а
английское изложение написано только на вступление и на строфы 1.1–1.6. Под
остальными 444 стоит одно слово: `Untranslated`.

Отсюда **две родословные у перевода**, и конвейер держит их порознь:

| папка | откуда взялся перевод |
|---|---|
| `ru/<гимн>.json` | по английскому изложению Габриэля Pradīpaka — как весь [/ksh/pv/](../pv/README.md) |
| `sa/<гимн>.json` | прямо с санскрита, здесь: изложения для этих строф нет вовсе |

Смешивать их молча нельзя — читатель вправе знать, чью работу он читает.
Подпись под страницей называет обе поимённо, с номерами строф, и считает их
сборка, а не человек. `check.py` следит, чтобы одна строфа не лежала в обеих
папках сразу.

## Шаги

```sh
python3 fetch.py           # исходная страница -> src/sv1005.html
python3 convert.py         # разбор вёрстки    -> blocks/<номер гимна>.json
python3 glossary.py        # словарь терминов  -> ../../ksh/sv/glossary/index.md
python3 render.py          # сборка страниц    -> ../../ksh/sv/chN/index.md
python3 audio.py           # озвучка терминов  -> ../../ksh/sv/audio/*.mp3
python3 toc.py             # оглавление        -> ../../ksh/sv/index.md
python3 check.py           # шесть тихих поломок, см. ниже
```

## Чем этот источник отличается от соседних

**Всё писание — одна страница.** Части здесь режутся не по файлам, а по
заголовкам «Chapter N» внутри одного разбора: у каждого гимна на странице свой
якорь `#ChapterN`, туда и ведёт ссылка «Эта часть у источника». Поэтому
`convert.py` тут свой, а не общий.

**Строфа и её транслитерация стоят в одном абзаце**: две строки деванагари, под
ними те же две строки IAST. В Parātrīśikāvivaraṇa они лежат двумя стенами, и
`pairing` в `common/page.py` складывает стены построчно. Здесь складывать
нечего, наоборот: абзац разводится надвое на разборе, чтобы тот же `pairing`
собрал его обратно уже построчно. Стен из-за этого выходит не одна на страницу,
а по одной на строфу, — поэтому `book.py` переопределяет `pairing` и оставляет
кнопки копирования одни на гимн: копируют здесь гимн целиком, а не строфу.

**Пометка `Untranslated` не показывается, но место под ней остаётся.** Двадцать
шесть раз подряд показать в рамке `pv-en` одно и то же слово — шум, а не
честность; выбросить абзац совсем — значит лишить перевод места, куда встать.
Поэтому на разборе от него остаётся пустой блок вида `gap`, помеченный номером
своей строфы. Пустым он молчит, переведённый встаёт обычным абзацем перевода.

**Ключ перевода — номер строфы, а не номер блока.** По номеру строфу зовут
(`13.11`), по нему на неё стоит якорь, и от правки разбора такой ключ не
съезжает — в отличие от порядкового номера блока, который съехал бы весь.

**У строфы есть свой адрес.** Номер берётся из хвоста её транслитерации
(`||13.11||`, а не `॥१३.११॥` над ним) и становится якорем: `/ksh/sv/ch13/#v13.11`.
Строфу здесь зовут по номеру — «Шивастотравали 13.11» цитирует
[«Тантралока»](/ksh/ta/), — и до сих пор такая ссылка вела в пустоту.

## Что где

| Файл | Что делает |
|---|---|
| `parts.py` | двадцать гимнов: названия, число строф по вступлению источника |
| `book.py` | чем «Шивастотравали» отличается от прочих писаний: две родословные перевода, кнопки, якоря |
| `fetch.py` | скачивает исходную страницу |
| `convert.py` | одна страница → двадцать наборов блоков; строфа разводится с транслитерацией |
| `render.py` | блоки + переводы → страницы Jekyll |
| `todo.py` | строфы, которым перевода ещё нет: санскрит и транслитерация рядом |
| `toc.py` | состав и состояние перевода в оглавление раздела |
| `add.py` | дописать переводы в названную папку, не потеряв уже написанное |
| `words.py` | сам словарь: статьи, формы слов, где термин звучит гуще всего |
| `glossary.py` | статьи + строфы → страница `/ksh/sv/glossary/` |
| `audio.py` | озвучка терминов с деванагари, голосом Lekha (общий скрипт) |
| `check.py` | шесть проверок |

Общее — в `tools/common/`: `parse.py` (вёрстка → блоки), `page.py` (блоки →
страница), `fetch.py` (загрузка), `check.py` (сверка текста).

`src/` и `blocks/` — промежуточные, в репозитории их нет: `fetch.py` и
`convert.py` восстанавливают их за минуту.

## Как переводить

```sh
python3 todo.py 2          # строфы второго гимна без перевода: санскрит и IAST
python3 todo.py 2 1 8      # только строфы 2.1–2.8
```

Перевод дописывается по номеру строфы, и папку надо назвать осознанно — от неё
зависит, чью работу увидит под страницей читатель:

```python
from add import add
add('sa', '2', {'2.1': 'русский текст…'})   # переведено прямо с санскрита
add('ru', '1', {'1.7': '…'})                # переведено по изложению Габриэля
```

Санскритское слово в переводе остаётся в скобке, как правит его рука; над
русским словом его поставит `render.py` на сборке. Скобки при этом не исчезают,
а уходят в `<rp>`: `/search-index.json` собирает Jekyll, снимая с абзаца теги, и
строка обязана совпасть с прежней знак в знак. Это и проверяет `check.py`.

Где чтение спорно, это говорится на месте — двойными дефисами, тем же способом,
каким Габриэль вставляет свои уточнения: `--в источнике «cintānām», родительный
падеж; читаю как «cintā nāma»--`. Замазать сомнение гладкой фразой здесь особенно
легко и особенно нечестно: сверить перевод не с чем.

## Что проверяет check.py

1. **Подстрочник не изменил текста** — общая проверка, `common/check.py`.
2. **Строф столько, сколько объявил источник.** Во вступлении Габриэль
   перечисляет их число по гимнам; на странице они лежат отдельно от этого
   перечня, и разойтись эти два счёта могут молча. Сейчас сходятся: 450.
3. **У каждой строфы есть транслитерация и номер.** Не сложилась пара — вместо
   строфы выйдут две стены; пропал номер — сломается и якорь `#v13.11`, и ключ,
   по которому лежит перевод.
4. **Перевод лежит при существующей строфе.** Ключ, которому на странице ничего
   не соответствует, — это перевод, которого никто никогда не увидит.
5. **Одна строфа — одна родословная.** Строфа, лежащая и в `ru/`, и в `sa/`,
   сделала бы подпись под страницей ложью: та называет номера поимённо.
6. **Каждый перевод — по-русски.** Абзац без кириллицы значит, что туда попал
   санскрит или английский, а не перевод.

## Словарь терминов

`/ksh/sv/glossary/` — восемьдесят две статьи, собранные не с нуля: санскритская
помета при каждом русском слове размечена единообразно, и список вынут из самого
перевода. Отбор — в `words.py`; там же формы слов (`bhaktiḥ`, `rasena`,
`nātham` — одна статья) и расчёт того, где термин звучит гуще всего.

Ссылки идут в обе стороны:

* из текста в словарь — сам подстрочник: `gloss()` в `tools/common/page.py`
  спрашивает у книги `link(слово)` и, если статья есть, оборачивает слово в
  ссылку. Со снятыми тегами строка при этом не меняется — иначе разошёлся бы
  поисковый указатель, и это проверяет `check.py`. Сейчас таких ссылок 438;
* из словаря в текст — **прямо в строфу, по её номеру**. Якорей для этого
  заводить не пришлось, и этим словарь отличается от соседнего: у
  Parātrīśikāvivaraṇa ссылки держатся на якорях, которые расставляет сборка
  страниц, а здесь у каждой строфы уже есть свой адрес (`#v13.11`).

`glossary.py` идёт **перед** `render.py`: страницы ставят ссылки на статьи
словаря, и список этих статей считает `words.py`.

Помета бывает составной (`bhakti-rasa-āsava-vara-unmadāḥ`). Ссылку такое слово
не получает — подстрочник сличает помету целиком, — но при счёте «где термин
звучит гуще всего» части считаются наравне: в гимнах понятия чаще всего и стоят
внутри составных слов. Без этого двадцать две статьи из восьмидесяти двух
остались бы без единой ссылки.

Озвучка — один комплект mp3 в `../../ksh/sv/audio/`, голосом Lekha (hi_IN).
Делает её `audio.py` (общий скрипт, `tools/common/audio.py`), и написание он
берёт из столбца «Деванагари» самой страницы словаря, а не переводит из IAST на
лету: у соседнего конвейера этот перевод делает библиотека, и она обращает `ṁ` в
ведический знак `ꣳ` — пять файлов там пришлось переснимать вручную.

Одно расхождение между написанием и звуком всё же есть, и оно нарочное. Хинди
глотает конечное краткое «а», и `शिव` выходил «шив» (0,34 с против 0,58 с у
`शिवः`). Поэтому слову, кончающемуся согласной, дописывается висарга — голос
произносит именительный падеж, `śivaḥ`, которым слово и называют вслух. Ценой
идёт лёгкое придыхание на конце; про это сказано в оговорке над таблицей.

## Вступление

Вступление переводчика переведено один раз на `/ksh/sv/`, откуда оно и взято.
В блоки оно не идёт: двадцати страницам оно ни к чему, а двадцать раз лежать в
поисковом указателе тем более не должно. Так же сделано у
[«Тантрасары»](../ts/README.md).

## Поиск

Отдельно ничего делать не надо. Указатель `/search-index.json` собирает сам
Jekyll, обходя страницы `.md`.
