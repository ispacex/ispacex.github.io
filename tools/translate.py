#!/usr/bin/env python3
"""Переводит страницы сайта при сборке и кладёт их в `/en/…`.

    python3 tools/translate.py --list          # что пойдёт в перевод, что нет
    python3 tools/translate.py --dry           # прогнать, ничего не покупая
    python3 tools/translate.py ksh/index.md    # одну страницу
    python3 tools/translate.py                 # всё

## Что переводится, а что нет

Переводится **наше**. Не переводится то, что само перевод:

* «[Тантралока](../ksh/ta/)» и «[Тантрасара](../ksh/tantrasara/)» по-русски —
  работа Габриэля Pradīpaka, перенесённая ради поиска;
* Parātrīśikāvivaraṇa и разбор Pratyabhijñāhṛdayam переведены **с его
  английского**.

Гнать это машиной обратно в английский значит выдать третье колено пересказа
вместо его собственного текста, который на одну ссылку дальше. Такие страницы
получают не перевод, а страницу со ссылкой на подлинник (см. `SECTION`,
`source()` и `stub()`).

Ссылка при этом **не сочиняется**: она уже стоит на каждой такой странице, и
здесь у неё меняется только язык.

Śivastotrāvalī — исключение и переводится: её русский сделан прямо с санскрита,
и по-английски её нет нигде. Но живёт она в `ru/*.json`, поэтому переводится не
здесь, а своим конвейером.

## Отчего страница режется на куски

Целую страницу модель переписывает: сбивает front matter, съедает вставки
Jekyll, «поправляет» таблицы. Кусок между пустыми строками — единица, которую
она возвращает целой, и та же единица, которой режет текст поисковый указатель.

Заголовок страницы (`title:`) переводится отдельно: это не проза, а строка в
кавычках, и вернуть её надо ровно строкой.

## Отчего кусок иногда переводится по частям

Кусок отвергается тем чаще, чем больше в нём разметки, а разметки больше всего
там, где текста меньше: в строфе с подстрочником каждое слово обёрнуто в
`<ruby>`, в словаре каждая статья — строка таблицы. Меток такому куску
достаётся сотнями, вернуть их все модель не может, и целая таблица остаётся
русской из-за одной потерянной.

Поэтому у куска три захода: целиком общим наказом, целиком наказом по виду
куска (`kindly`) и — если не далось — **по частям**: переводится то, что лежит
между метками, по одной части, и метки к модели не едут вовсе. Потерять их
тогда нельзя. Цена — часть переводится, не видя соседей; оттого это и
последнее средство, а не первое.

## Отчего запросы идут разом

Кусков на сайте почти шесть тысяч. По одному запросу за раз это часы ожидания
на пустом месте: время уходит не на счёт, а на дорогу до сервера и обратно.
Поэтому сперва собирается список всего, что предстоит купить, из него
выбрасываются повторы (одна и та же шапка стоит на четырёх десятках страниц), и
оставшееся забирается в несколько потоков. Собирается страница уже из готового
кеша, по порядку и без сети.

## Ссылки внутри

Ссылка на страницу, у которой английский двойник есть, ведёт к двойнику; на ту,
у которой нет, — остаётся русской. Это честно: страница и правда русская, и
подменять её нечем. Оттого перевод идёт в два прохода — сперва становится
известен состав английского дерева, потом правятся ссылки.
"""
import argparse
import concurrent.futures
import os
import re
import sys
import threading

HERE = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.dirname(HERE)
sys.path.insert(0, HERE)

from common.translate import (Cache, SLOT, SLOT_RE, fingerprint, mask,
                              translate, unmask)

# Слово из двух алфавитов ловит `check-scripts.py` — та же проверка, что стоит
# на сайте с давних пор. Здесь она нужна раньше: перевод рождает такие слова
# сам. «урас» модель наполовину переложила латиницей и вернула «уras» —
# кириллическая «у», латинское «ras». На странице это выглядит словом, а
# поиском не находится ни по одному написанию.
import importlib.util as _il

_spec = _il.spec_from_file_location('chk', os.path.join(HERE, 'check-scripts.py'))
_chk = _il.module_from_spec(_spec)
_spec.loader.exec_module(_chk)


def mixed(text):
    """Слова, в которых сошлись два алфавита. Пусто — значит чисто."""
    return [m.group(0) for m in _chk.WORD.finditer(text) if _chk.strangers(m.group(0))]

OUT = os.path.join(ROOT, 'en')
LANG = 'English'
CODE = 'en'

# Разделы, чей русский сам перевод. Их страницы не переводятся; вместо этого
# каждая получает свою английскую страницу со ссылкой на подлинник.
#
# `name` — имя раздела: оно в IAST и одинаково на обоих языках. `home` — весь
# раздел у источника. `made` — чей русский лежит на этих страницах: `his` —
# собственный русский Габриэля, перенесённый без изменений; `here` — сделанный
# для этого сайта с его английского. Разница видна читателю и потому написана
# на странице разными словами.
#
# Адреса заглавных сверены по заголовку страницы у источника, а не выписаны на
# глаз: выписанный на глаз уже был неверен — у Parātrīśikāvivaraṇa здесь стоял
# узел 793, а это «строфы 1–2, часть 4», а не введение.
SECTION = {
    'ksh/pv/': ('Parātrīśikāvivaraṇa',
                'https://www.sanskrit-trikashaivism.com/en/node/540', 'here'),
    'ksh/ta/': ('Tantrāloka',
                'https://www.sanskrit-trikashaivism.com/en/node/581', 'his'),
    'ksh/tantrasara/': ('Tantrasāra',
                        'https://www.sanskrit-trikashaivism.com/en/node/919', 'his'),
    'ksh/ph/': ('Pratyabhijñāhṛdayam',
                'https://www.sanskrit-trikashaivism.com/en/node/543', 'here'),
}

# Ссылка на подлинник не сочиняется. Она уже стоит на каждой из этих страниц —
# в хлебных крошках («Эта часть у источника») или в подписи под текстом, — и
# берётся оттуда; здесь у неё меняется только язык.
#
# Так можно потому, что узел у Габриэля **один на оба языка**: `/ru/node/869` и
# `/en/node/869` — две версии одной и той же девятой главы. Значит перевод
# адреса не может привести не на ту страницу. Считать адрес по номеру главы,
# наоборот, нельзя: номера узлов идут с пропусками (869, 871, 873, 878…), и
# арифметика попала бы в соседний текст, ничем не выдав ошибки.
SRC = re.compile(r'https://www\.sanskrit-trikashaivism\.com/(ru|en)/(?:[^)"\s]+/)?(\d+)(#[^)"\s]*)?')


def source(text):
    """Английский подлинник этой страницы у Габриэля. Ссылки нет — None."""
    m = SRC.search(text)
    if not m:
        return None
    lang, node, frag = m.groups()
    if lang == 'en':
        return m.group(0)
    return 'https://www.sanskrit-trikashaivism.com/en/node/%s%s' % (node, frag or '')


def section(rel):
    """(префикс раздела, имя, заглавная у источника, чей русский)."""
    for pref, row in SECTION.items():
        if rel.startswith(pref):
            return (pref,) + row
    return None, None, None, None

# Страницы этих разделов — сам перенесённый перевод. Заглавная страница раздела
# и словарь написаны здесь и переводятся.
ROUND = ('ksh/pv/s', 'ksh/ta/ch', 'ksh/tantrasara/ch', 'ksh/ph/s', 'ksh/ph/begin')

# Что не идёт в перевод вовсе: служебное, не страницы.
SKIP = ('_sitecheck/', 'sitesearch/', 'tools/', '.claude/', 'en/',
        'search-index/', 'search/', 'dance/search.md', 'CLAUDE.md')

# Двойник, написанный руками. Страница поиска машине не отдаётся (она наполовину
# из <script>, и переводить в ней надо не прозу, а обещания движку), но
# английская пара у неё есть — en/search/index.md, и правится она там же.
#
# Названа она здесь затем, чтобы ссылки с английских страниц вели к ней, а не на
# русский поиск, и чтобы переключатель языка на самом поиске знал, куда вести.
# Сама страница помечена `byhand: true` — по этой пометке её обходит
# tools/check-i18n.py, который сличает перевод с исходником построчно.
# Иначе выходило бы обидное: перевод в указателе есть и по языку отбирается, а
# прийти за ним читателю некуда — тридцать шесть английских страниц отправляют
# его на русский поиск, где английских находок нет по построению.
BYHAND = ('/search/',)

# Что во front matter принадлежит переводу, а не странице: заголовок он
# переводит, язык объявляет, обратный адрес считает. Всё остальное — своё у
# страницы, и до двойника обязано доехать.
OURS = ('title', 'lang', 'ru')

# А это не доедет никогда. `permalink` — это адрес: доехав, он заставил бы обе
# страницы просить один и тот же, и Jekyll отдал бы его одной из двух, молча.
NEVER = ('permalink',)

# Поля, в которых лежит проза, а не признак: их переводим, а не переносим.
# `description` читает `{% seo %}` — он уезжает в выдачу поисковика и в карточку
# ссылки, то есть остаётся русским ровно там, где виден чаще самой страницы.
PROSE = ('description',)

# Строка front matter вида `имя: значение`. Продолжений и вложенности здесь не
# бывает: front matter на этом сайте плоский (проверено по всем страницам).
FMLINE = re.compile(r'^([A-Za-z_][\w-]*):\s*(.*)$')

CYR = re.compile(r'[А-Яа-яЁё]')
# Санскритская помета — та же, что маскируется при переводе. Здесь она нужна,
# чтобы сличить её последовательность до и после.
MARK = re.compile(r'\([A-Za-zĀ-ſḀ-ỿ\'\-\s.…|]+\)')
# Всё, что стоит в скобках, — и помета, и слово, вставленное переводчиком. Теги
# при этом снимаются: подстрочник написан `<ruby>слово<rp> (</rp><rt>…`, и
# скобка там разорвана разметкой пополам.
TAG = re.compile(r'<[^>]+>')
GROUP = re.compile(r'\(([^()]*)\)')
FM = re.compile(r'\A---\n(.*?)\n---\n', re.S)
TITLE = re.compile(r'^title:\s*"?(.*?)"?\s*$', re.M)
FENCE = re.compile(r'\A```')
LINK = re.compile(r'\]\((/[^)]*)\)')
# Ссылка бывает и разметкой Markdown, и готовым тегом: перечни глав на
# заглавных страницах разделов написаны прямо на HTML. Пока правилось только
# первое, английская страница «Pratyabhijñāhṛdayam» уводила читателя в русские
# афоризмы, хотя английские двойники у них были.
HREF = re.compile(r'href="(/[^"]*)"')


def pages():
    """Страницы сайта: (путь, что с ней делать, пояснение).

    Три состояния, а не два: `ours` — перевести, `source` — не переводить, а
    поставить ссылку на подлинник, `no` — не трогать вовсе.
    """
    out = []
    for base, dirs, files in os.walk(ROOT):
        dirs[:] = [d for d in dirs if not d.startswith('.')]
        for f in sorted(files):
            if not f.endswith('.md'):
                continue
            rel = os.path.relpath(os.path.join(base, f), ROOT)
            if rel.startswith(SKIP):
                continue
            text = open(os.path.join(ROOT, rel), encoding='utf-8').read()
            if not CYR.search(text):
                out.append((rel, 'no', 'не по-русски'))
            elif rel.startswith(ROUND):
                out.append((rel, 'source', 'сама перевод — ссылка на подлинник'))
            else:
                out.append((rel, 'ours', ''))
    return out


def title_of(rel):
    """Заголовок страницы из front matter. Нет — None."""
    src = open(os.path.join(ROOT, rel), encoding='utf-8').read()
    m = FM.match(src)
    if not m:
        return None
    t = TITLE.search(m.group(1))
    return t.group(1) if t else None


def twin(rel):
    """Адрес английского двойника: `ksh/index.md` -> `/en/ksh/`."""
    url = '/' + rel[:-len('.md')]
    if url.endswith('/index'):
        url = url[:-len('index')]
    return '/en' + url


# Имя, которое перевела не модель, а сам сайт. «КШ» — сокращение от
# «кашмирский шиваизм»; на просьбу перевести его модель отвечает «КШ», и оно
# так и стояло в хлебных крошках сорока трёх страниц. Как этот раздел зовётся
# по-английски, решено не здесь: в `stub()` та же ссылка написана руками —
# «Kashmir Shaivism». Подставляется имя **до** перевода, а не после: тогда оно
# стоит по-английски и в куске, который перевод отверг.
NAMES = ((re.compile(r'\bКШ\b'), 'Kashmir Shaivism'),)


def blocks(body):
    """Куски между пустыми строками, как их режет поисковый указатель."""
    for pat, name in NAMES:
        body = pat.sub(name, body)
    return re.split(r'\n\s*\n', body)


def relink(text, have):
    """Ссылки внутрь сайта — на английские двойники, где они есть.

    Якорь отделяется и возвращается на место: `/ksh/ta/ch9/#v9.5` — та же
    страница, что `/ksh/ta/ch9/`, и без этого она бы не узналась.

    Косая черта в конце тоже отделяется, и по той же причине. Для Jekyll
    `/dance` и `/dance/` — одна страница, а сличение строка в строку считало
    их разными, и написанное без черты двойника не находило. Так английский
    читатель уходил в русский текст с восьмидесяти двух ссылок — включая **всю
    навигацию английской главной**: девять разделов из девяти написаны там без
    черты. Подставляется при этом тот вид адреса, что записан в `have`, а не
    тот, что стоял в ссылке: у Jekyll он канонический, и лишнего перехода по
    дороге не будет.
    """
    canon = {u.rstrip('/'): u for u in have}

    def swap(url):
        base, sep, frag = url.partition('#')
        twin = canon.get(base.rstrip('/'))
        return ('/en' + twin + sep + frag) if twin else url

    text = LINK.sub(lambda m: '](%s)' % swap(m.group(1)), text)
    return HREF.sub(lambda m: 'href="%s"' % swap(m.group(1)), text)


def looks(en, ru):
    """Похоже ли это на перевод строки, а не на разговор с моделью.

    Проза, вернувшаяся не тем, чем была, видна глазами: абзац на странице
    читают. Строка front matter не видна нигде — её читает механика, и потому
    сличать её приходится здесь.

    Четыре признака, и каждый нашёлся на сайте: пусто или метки не вернулись
    (заголовок страницы пожертвований — модель приписала к нему `⟦0⟧`, которого
    в исходнике не было); перевод в несколько строк; кириллица (модель
    пересказывает исходник, цитируя его); длина не по чину (два заголовка глав
    «Натьяшастры» уехали на сайт целым абзацем «I need the source text to
    translate…»). Пятый — числа: номер главы в заголовке не украшение, по нему
    читатель сверяет, туда ли попал, и по нему же главы выстраиваются в палитре.
    """
    if not en or not en.strip():
        return False
    if '\n' in en.strip():
        return False
    if CYR.search(en):
        return False
    if len(en) > 2 * len(ru) + 24:
        return False
    return re.findall(r'\d+', en) == re.findall(r'\d+', ru)


# Кириллица, какой её видит эта проверка: слово целиком, хоть в одну букву
# («О» — тоже слово подстрочника).
CYRW = re.compile(r'[А-Яа-яЁё]+')
# Разметка, с которой строка начинается: решётка заголовка, знак списка, знак
# цитаты, край строки таблицы. Модель её то и дело съедает — «### Полная
# книга» вернулось как «Full book», «# Гимн 2» как «## Hymn 2», — и заголовок
# на странице становится абзацем, а цитата теряет отступ. Отступ здесь
# неразрывными пробелами: главы «Натьяшастры» набраны ими. Знак списка
# считается знаком только с пробелом после: без пробела это звёздочка
# выделения, попавшая на начало перенесённой строки.
LEAD = re.compile(r'^[ \t\xa0]*(#{1,6} |>+|[*+-] |\d+\. |\|)', re.M)
# Выделение, с которого начинается сам кусок. Заголовок части здесь бывает не
# решёткой, а курсивом: «*Шейные украшения*» вернулось как «Neck ornaments», и
# на странице подпись перестала быть подписью.
OPEN = re.compile(r'\A[\s]*([*_]{1,3})(?=\S)')


def marks_run(text):
    """Разметка начал строк подряд идущими одинаковыми — за одну.

    Цитата в шесть строк и та же цитата, переписанная в четыре, размечены
    одинаково: `>` в начале каждой. Считать их порознь значило бы объявить
    поломкой всякий перенос строки, а перевод переносит их сам собой.
    """
    out = []
    for m in LEAD.findall(text):
        if not out or out[-1] != m:
            out.append(m)
    return out


def opens(text):
    m = OPEN.match(text)
    return m.group(1) if m else ''


def framed(en, ru):
    """Одинаково ли размечены строки: заголовки, списки, цитаты, таблица."""
    return marks_run(en) == marks_run(ru) and opens(en) == opens(ru)


def answered(en, ru):
    """Похоже ли это на перевод куска, а не на разговор с моделью.

    Кусок — не страница: страница режется по пустым строкам, и куском бывает
    заголовок в два слова, строка таблицы или обрывок фразы («поста.», «лое,»,
    «корзи-» — вторая глава «Натьяшастры» набрана с переносами). На таком входе
    модель то и дело отвечает не переводом, и оба её ответа уезжали на страницу
    как есть: исходник слово в слово (55 кусков) и её собственная реплика («I
    need the source text to translate. Please provide the Russian text…» — это
    стояло в `/en/ship/`, в `/en/theatre/abhinavagupta/` и в десяти местах
    второй главы).

    Признака три, и все — по замаскированному: неприкосновенное до модели не
    доходит и вернуться переведённым не может. В словарях внутри тегов законно
    стоит русское (`data-alias="санкоча"`), и по неприкосновенному эта проверка
    ругалась бы на семь десятков безупречных переводов.

    **Исходник слово в слово.** Самый частый отказ и самый тихий: страница
    собирается, читается, и русским остаётся ровно то, что модель поленилась
    тронуть, — строка хлебных крошек на двадцати гимнах, перечень разделов на
    главной. Раньше он ловился заодно с кириллицей, теперь назван своим именем.

    **Кириллица, которой в исходнике не было.** Не всякая кириллица в ответе —
    отказ: на `/ksh/ta/` подсказка под полем поиска нарочно показывает, что
    «шактипата» найдёт `śaktipāta`, и правильный перевод обязан это слово
    сохранить. Пока правило гласило «кириллицы быть не должно вовсе», оно
    отвергало безупречный перевод, и подсказка оставалась русской целиком —
    все пятьдесят её слов. Отказ — это **своя** кириллица (модель пересказывает
    исходник, цитируя его) или русским оставшаяся заметная часть куска. Мера
    здесь — десятая: перевод, сохранивший пример, оставляет одно слово из
    пятидесяти («шактипата» в подсказке к поиску), а отказ оставляет русским
    всё. Одно из десяти уже не пример: в «Абиддха (ābiddha, «разбивание»)»
    русским осталось толкование, и на второй заход модель перевела его тоже. На
    коротком куске десятая не оставляет ничего, и это по делу: в строке «КШ ·
    Вернуться на главную» уцелевшее «КШ» — не пример, а непереведённая четверть
    строки, и разбор по частям переводит её тоже.

    **Длина не по чину.** Реплика о том, что текст неполон, длиннее самого
    куска в разы: «Вити» — четыре знака, ответ — сто девять. Правило кусается
    только на коротком входе, где такие ответы и живут: на куске в две тысячи
    знаков вдвое длиннее не бывает ни перевода, ни отговорки. Проверено по всем
    5841 купленным кускам: под правило попали тринадцать, и все тринадцать —
    разговор, ни одного перевода.
    """
    if not en or not en.strip():
        return False
    was = CYRW.findall(mask(ru)[0])
    if not was:
        # Переводить было нечего: вся кириллица куска — внутри разметки. Такой
        # кусок возвращается как есть, и это правильный ответ, а не эхо.
        return True
    if en.strip() == ru.strip():
        return False
    left = CYRW.findall(mask(en)[0])
    if set(left) - set(was):
        return False
    if len(left) * 10 >= len(was):
        return False
    return len(en) <= 2 * len(ru) + 20


def remnant(en):
    """Сколько русских слов осталось в переводе. Считается по замаскированному:
    внутри тегов русское законно и переводом не тронуто."""
    return len(CYRW.findall(mask(en)[0]))


def moved(en, ru):
    """Разъехались ли пометы: потерялась, прибавилась или переехала.

    Сличаются **все** скобки по порядку, а не одни пометы, и в этом вся суть.
    Скобка на этих страницах бывает двух родов, и по-русски их различает
    алфавит: латиница в скобках — санскрит соседнего слова (`(evam eva)`),
    кириллица — слово, вставленное переводчиком для связности (`_(и)_`,
    `_(преданным)_`). Помета обязана доехать неизменной; вставленное слово
    обязано перевестись — оно затем и вставлено, чтобы его читали.

    Пока сличались одни пометы, перевод вставленного слова выглядел прибавленной
    пометой и отвергался вместе со всей строфой: пятая строфа первого гимна
    оставалась русской из-за трёх таких слов. А сличение по порядку ловит
    больше прежнего: помета, потерянная **или переехавшая** на другое место,
    теперь видна тоже.
    """
    a, b = GROUP.findall(TAG.sub('', ru)), GROUP.findall(TAG.sub('', en))
    if len(a) != len(b):
        return True
    return any(x != y for x, y in zip(a, b) if MARK.fullmatch('(%s)' % x))


# Буква или цифра: по ним решается, стояло ли между тегами слово. Знаки и
# пробелы не в счёт — края пояснений и разделители переезжают законно (`EDGE`).
WORDCH = re.compile(r'[^\W_]', re.U)
# Разметка, по которой считается расстановка: тег и вставка Jekyll. Не всё
# маскируемое (`KEEP`): по санскритской помете и коду считать нельзя — помету
# перевод законно прибавляет там, где по-русски стояло тире, и на рукописных
# страницах «Натьяшастры» таких мест сотни. Правило ловит переезд через **тег**,
# и границы у него теговые: 869 отвергнутых кусков против 16, и все 16 — по делу.
FRAME = re.compile(r'<[^>]+>|\{%.*?%\}|\{\{.*?\}\}', re.S)


def spread(text):
    """Где между тегами стоит слово, а где нет: по одному «да/нет» на промежуток.

    Метки возвращаются все, по разу и в том же порядке — это стережёт
    `unmask`, — но текст между ними модель переставляет: слово, стоявшее внутри
    тега, оказывается снаружи, а тег остаётся пустым.

    На английских гимнах Śivastotrāvalī так уехало санскритское слово из своей
    ссылки — `<rt>ātmā<a class="pv-gl"></a></rt>` вместо `<rt><a …>ātmā</a></rt>`:
    ссылки не видно и не нажать. Там же подстрочник встал не над своим словом:
    `homage<ruby> (<rp></rp><rt>namaḥ</rt>…` — основа осталась снаружи `<ruby>`,
    а внутри него оказалась скобка. Девять страниц, 95 мест (VS-53).

    Ни одна прежняя проверка этого не видела: меток столько же, скобки на
    местах, разметка строк цела, русского не осталось. Видна только сама
    расстановка.
    """
    return tuple(bool(WORDCH.search(g)) for g in FRAME.split(text))


# Мера безупречного: разметка цела, русского не осталось.
BEST = (0, 0)


def worse(en, ru):
    """Чем этот перевод хуже безупречного. Ноль — ничем.

    Мерок две, и они не равны по весу. Съеденная разметка строк тяжелее:
    заголовок без решётки становится на странице абзацем, и палитра перехода
    его больше не видит. Русский остаток легче: слово осталось словом.
    Сравнивать так можно потому, что оба ответа уже признаны переводом
    (`answered`); здесь выбирается лучший из годных, а не годный из всех.
    """
    return (0 if framed(en, ru) else 1, remnant(en))


def kindly(b):
    """Каким наказом переспрашивать этот кусок.

    Кусок в одну строку — не абзац: это заголовок, строка хлебных крошек,
    строка таблицы. Наказ «это часть страницы, целого не будет» (`again`) на
    таком входе не помогает — «Вити» он возвращал как «Вити», — а наказ о
    коротком куске (`bit`) переводит: «Обход» → «Circumambulation», «Словарь
    терминов» → «Glossary of terms».
    """
    return 'bit' if '\n' not in b.strip() else 'again'


def fits(en, ru, ruby=False):
    """Годится ли перевод **части** куска — подписи, ячейки, слова подстрочника.

    Строже, чем `answered`, и по делу: часть возвращается на своё место внутри
    разметки, и промах здесь виден не абзацем, а строкой таблицы.

    Своя метка. На коротком входе модель охотно отвечает `⟦0⟧` — меткой,
    которой в вопросе не было (так вышло с «Обходом», «Бханой», «Пушпагандикой»
    и ещё девятью заголовками «Натьяшастры»). В части меток нет вовсе, значит
    любая — выдумана.

    Своя скобка. Считается их число: часть — не абзац, и сличать нечего. А в
    строфе с подстрочником сличается и порядок помет (`moved`): часть стоит
    между ними, и переехавшая помета — это съехавший подстрочник.

    Свой перевод строки. Часть стоит внутри строки таблицы или тега; лишний
    перенос разорвал бы разметку вокруг неё.
    """
    if not en or not en.strip():
        return False
    if SLOT_RE.search(en):
        return False
    if en.count('(') != ru.count('(') or en.count(')') != ru.count(')'):
        return False
    if ruby and moved(en, ru):
        return False
    if en.count('\n') != ru.count('\n'):
        return False
    return answered(en, ru)


# Края части — не текст, а разметка вокруг него: тире по бокам пояснения
# переводчика, точка-разделитель хлебных крошек, скобка ссылки. Модель их то
# теряет, то переставляет, а вернуть их на место можно и не спрашивая: они
# отрезаются до вопроса и приставляются обратно после.
EDGE = ' \t\n—–·|>[*'


def bit(text, cache, stats, ruby=False):
    """Перевод одной части. Не далось — часть остаётся русской."""
    body = text.strip(EDGE)
    if not CYR.search(body):
        return text
    head = text[:text.index(body)]
    tail = text[text.index(body) + len(body):]
    got = translate(body, LANG, cache, stats, kind='bit')
    if got is None or not fits(got, body, ruby):
        stats['bits'] = stats.get('bits', 0) + 1
        return text
    return head + got + tail


def piecemeal(b, cache, stats):
    """Последнее средство: перевести то, что лежит **между** метками.

    Кусок отвергается тем чаще, чем больше в нём разметки, и разметки больше
    всего там, где текста меньше всего: в строфе с подстрочником каждое слово
    обёрнуто в `<ruby>`, в словаре каждая статья — строка таблицы. Метки такому
    куску достаются сотнями: 421 в таблице словаря Pratyabhijñāhṛdayam, 202 в
    гимне, 134 в оглавлении Tantrasāra. Вернуть их все, по разу и в том же
    порядке, модель не может — и целая таблица остаётся русской из-за одной
    потерянной метки. Так на английских страницах стояло 1441 русское слово.

    Здесь модель не видит меток вовсе: ей отдаётся по одной части — слово
    подстрочника, ячейка, подпись ссылки, — а метки остаются дома. Потерять их
    поэтому нельзя: они не уезжали.

    Цена — потерянная связь между частями: часть переводится, не видя соседей.
    Оттого это и последнее средство, после того как кусок не дался целиком ни
    с первого раза, ни с переспроса. А там, где кусок и есть перечень
    несвязанных частей — таблица, оглавление, подстрочник, — цены нет вовсе:
    ячейка соседке и не родня.

    Часть, которая не далась, остаётся русской — по тому же правилу, что и
    кусок: испорченное чинить нечем, а по-русски оно хотя бы не врёт.
    """
    masked, parts = mask(b)
    ruby = 'pv-tr' in b or 'pv-pair' in b or '<ruby' in b
    out = []
    for n, piece in enumerate(SLOT_RE.split(masked)):
        out.append(SLOT % int(piece) if n % 2
                   else bit(piece, cache, stats, ruby))
    return unmask(''.join(out), parts)


# Что читатель видит внутри тега. Тег маскируется целиком — иначе модель
# правит класс и адрес, — и подпись внутри него до перевода не доходит вовсе:
# на английском словаре поле поиска предлагало «начните вводить термин,
# например «чити»», а над каждой из 1197 ссылок «где в тексте» всплывало
# «Афоризм 7». Поэтому подписи переводятся отдельно и после, по одной, и тега
# модель по-прежнему не видит.
#
# Названы ровно четыре атрибута, и только они. Соседние русские значения —
# `data-alias="санкоча"`, `data-tts="citi"` — читателю не видны и трогать их
# нельзя: по первому фильтр словаря находит статью, по второму зовётся звуковой
# файл. Правило «переведём всё русское внутри тега» сломало бы оба.
SHOWN = re.compile(r'\b(placeholder|title|alt|aria-label)="([^"]*)"')


def labels(text):
    """Подписи внутри тегов, которые читатель видит."""
    return [m.group(2) for m in SHOWN.finditer(text) if CYR.search(m.group(2))]


def relabel(text, cache, stats):
    """Те же подписи — по-английски. Не далась — остаётся русской."""

    def one(m):
        name, value = m.group(1), m.group(2)
        if not CYR.search(value):
            return m.group(0)
        got = translate(value, LANG, cache, stats, kind='bit')
        if got is None or not fits(got, value):
            stats['labels'] = stats.get('labels', 0) + 1
            return m.group(0)
        # Прямая кавычка в подписи оборвала бы сам атрибут. Русский оригинал
        # обходится ёлочками, а перевод их не обязан повторять: `«шакти»`
        # вернулось как `"śakti"`. Это не порча — это разметка, и пишется она
        # тем, чем положено.
        return '%s="%s"' % (name, got.replace('"', '&quot;'))

    return SHOWN.sub(one, text)


def gotten(t, cache, kind=None):
    """Готовый перевод куска из кеша, если он там есть и годится. Иначе None."""
    masked, parts = mask(t)
    got = cache.get(fingerprint(masked, cache.lang + (':' + kind if kind else '')))
    if got is None:
        return None
    back = unmask(got, parts)
    return back if back is not None and answered(back, t) else None


def tries(b):
    """Наказы, какими кусок спрашивают повторно, — по порядку."""
    first = kindly(b)
    return (first, 'again' if first == 'bit' else 'bit')


def waiting(texts, cache):
    """Кому нужен ещё заход, и каким наказом: наказ → куски.

    Кусок, потерявший метки, стоит здесь наравне с прочими. Раньше он выпадал
    молча — ответ не разворачивался, и до сличения дело не доходило, — а это
    самый частый отказ из всех: 45 кусков, 1441 русское слово. Переспрос не
    чинит потерянную метку и не пытается: он спрашивает ещё раз, другим наказом.
    """
    out = {}
    for t in texts:
        got = gotten(t, cache)
        if got is not None and not remnant(got):
            continue
        for kind in tries(t):
            more = gotten(t, cache, kind)
            if more is not None and not remnant(more):
                break
            if cache.get(fingerprint(mask(t)[0], cache.lang + ':' + kind)) is None:
                out.setdefault(kind, []).append(t)
                break
    return out


def hopeless(texts, cache):
    """Куски, которым не помог и переспрос: их разберут на части.

    Возвращаются сами части — то, что предстоит купить, — а не куски: покупка
    идёт разом, а собирается страница потом, по кешу.
    """
    out = []
    for t in texts:
        ready = [g for g in (gotten(t, cache, k) for k in (None,) + tries(t))
                 if g is not None]
        if any(sound(g, t) is None for g in ready):
            continue
        masked, _parts = mask(t)
        for n, piece in enumerate(SLOT_RE.split(masked)):
            if not n % 2 and CYR.search(piece.strip(EDGE)):
                out.append(piece.strip(EDGE))
    return out


def phrase(ru, cache, stats):
    """Строка front matter по-английски: заголовок, описание. Не вышло — None.

    Спрашивается дважды. Первый раз — общим наказом, тем же, каким переводится
    проза: его ответы уже куплены, и переспрашивать все полторы сотни заголовков
    ради трёх испорченных незачем. Ответ, на строку не похожий, отвергается, и
    тогда — второй раз, назвав вещь своим именем (`kind='title'`): «это
    заголовок страницы, а не просьба к тебе». Купится при этом только то, что и
    правда испорчено.
    """
    got = translate(ru, LANG, cache, stats)
    if looks(got, ru):
        return got
    stats['reask'] = stats.get('reask', 0) + 1
    got = translate(ru, LANG, cache, stats, kind='title')
    return got if looks(got, ru) else None


def wanted(rel):
    """Куски страницы, которые пойдут в перевод: (строки шапки, куски текста).

    Врозь потому, что спрашиваются они по-разному, когда ответ не годится:
    строка шапки переспрашивается заголовком (`phrase`), кусок текста — куском
    (`answered`). Свалив их в одну кучу, переспрашивали бы заголовок дважды и
    оба раза не тем наказом.
    """
    src = open(os.path.join(ROOT, rel), encoding='utf-8').read()
    m = FM.match(src)
    head, body = (m.group(1), src[m.end():]) if m else ('', src)
    title = TITLE.search(head)
    top = [title.group(1)] if title else []
    for line in head.split('\n'):
        f = FMLINE.match(line)
        if f and f.group(1) in PROSE:
            top.append(f.group(2).strip().strip('"'))
    out = []
    for b in blocks(body):
        if b.strip() and CYR.search(b) and not FENCE.match(b.strip()):
            out.append(b)
    return top, out


def warm(texts, cache, workers=8, kind=None):
    """Покупает всё, чего в кеше нет, — в несколько потоков.

    Кеш до и после — единственное, что связывает потоки, и пишут они в него
    под замком. Сборка страницы после этого идёт по кешу и сети не касается:
    так порядок кусков на странице не зависит от того, в каком порядке
    отвечал сервер.
    """
    from common.translate import ask, fingerprint, mask
    need = {}
    for t in texts:
        masked, _parts = mask(t)
        if not re.search(r'[^\W\d_]', re.sub(r'⟦\d+⟧', '', masked), re.U):
            continue
        # Ключ — у кеша, тот же, что берёт `translate()` при сборке. Пока
        # здесь стояло своё имя языка, закупка и сборка считали разные ключи, и
        # всё покупалось дважды.
        fp = fingerprint(masked, cache.lang + (':' + kind if kind else ''))
        if cache.get(fp) is None:
            need[fp] = masked
    if not need:
        return 0
    lock = threading.Lock()
    done = [0]

    def one(item):
        fp, masked = item
        got = ask(masked, LANG, kind=kind)
        with lock:
            cache.put(fp, got)
            done[0] += 1
            if done[0] % 100 == 0:
                cache.save()
                print('   куплено %d из %d' % (done[0], len(need)), flush=True)

    with concurrent.futures.ThreadPoolExecutor(max_workers=workers) as pool:
        list(pool.map(one, list(need.items())))
    cache.save()
    return len(need)


def whole(b, cache, stats):
    """Перевод куска целиком — или None, если ответ переводом не был.

    Наказов два, и спрашиваются оба: сперва тот, что подходит виду куска
    (`kindly`), потом другой. Разница между ними не косметическая — «это часть
    страницы, целого не будет» и «это короткий кусок: заголовок, подпись,
    ячейка», — и кусок, на который модель огрызнулась по первому, по второму
    отвечается переводом. «Тримудхака» так и осталась русской на английской
    странице, хотя перевод её был куплен: спросили не тем наказом.

    Повод спросить ещё раз — не только отказ, но и **русский остаток** или
    **съеденная разметка** (`worse`): ответ годный, а слово-другое в нём
    осталось по-русски или заголовок вернулся без решётки. Иногда остаток по
    делу («шактипата» — пример запроса в подсказке к поиску), а иногда это
    просто непереведённое толкование, и другой наказ переводит его тоже.
    Поэтому из годных ответов берётся лучший, а первый же безупречный кончает
    поиск.
    """
    got = translate(b, LANG, cache, stats)
    best = got if got is not None and answered(got, b) else None
    if best is not None and worse(best, b) == BEST:
        return best
    stats['again'] = stats.get('again', 0) + 1
    for kind in tries(b):
        more = translate(b, LANG, cache, stats, kind=kind)
        if more is None or not answered(more, b):
            continue
        if best is None or worse(more, b) < worse(best, b):
            best = more
        if worse(best, b) == BEST:
            break
    return best


def sound(got, b):
    """Чем перевод куска плох, если он плох. Всё хорошо — None.

    Три тихие поломки: каждая оставляет страницу целой на вид, и заметить их
    можно только сличением с исходником.

    **Помета.** Модель не только теряет — она добавляет: там, где в русском
    стояло тире, в английском появляется пояснение в скобках. На рукописной
    странице скобка это скобка, и вреда нет. Там, где из скобок строится
    подстрочник, `(lit. …)` встало бы мелким шрифтом над соседним словом, будто
    это санскрит. Подстрочник ищется в самом куске: подпись под гимном скобок
    не строит, строфа над ней — строит. Как именно сличаются скобки — в
    `moved`.

    **Два алфавита.** «урас» модель наполовину переложила латиницей и вернула
    «уras». На странице это слово, а поиском оно не находится никак.

    **Непарная звёздочка.** Она портит не абзац, а всё, что ниже: полужирное
    начинается и не кончается до конца страницы. Сличается чётность до и
    после, а не сама чётность: кое-где звёздочка непарна и по-русски.

    **Переехавший через метку текст.** Слово, стоявшее внутри тега, вышло
    наружу, а тег остался пустым (`spread`). Кусок с такой бедой уходит
    разбираться по частям, а там метки не уезжают вовсе: они и не покидали
    дома.
    """
    ruby = 'pv-tr' in b or 'pv-pair' in b or '<ruby' in b
    if ruby and moved(got, b):
        return 'marks'
    if spread(got) != spread(b):
        return 'shifted'
    if mixed(got) and not mixed(b):
        return 'mixed'
    if got.count('**') % 2 != b.count('**') % 2:
        return 'bold'
    return None


def do(rel, cache, stats, dry=False):
    """Переводит страницу. Кусок, который не дался, остаётся русским.

    У куска три захода: целиком (`whole`, двумя наказами) и по частям
    (`piecemeal`). Что вышло, проверяется на три тихие поломки (`sound`), и
    поломка — тоже повод разобрать кусок на части: помета, которую модель
    приписала целому абзацу, в отдельно взятой части не появляется.

    Не далось ни так, ни этак — кусок остаётся русским. Испорченное место
    чинить нечем, а по-русски оно хотя бы не врёт.
    """
    src = open(os.path.join(ROOT, rel), encoding='utf-8').read()
    m = FM.match(src)
    head, body = (m.group(1), src[m.end():]) if m else ('', src)

    title = TITLE.search(head)
    if title and dry:
        en_title = title.group(1)
    elif title:
        en_title = phrase(title.group(1), cache, stats)
        if en_title is None:
            stats['titles'] = stats.get('titles', 0) + 1
            en_title = title.group(1)
    else:
        en_title = None

    # Что страница объявила о себе сверх заголовка: `layout`, `search`, всё
    # прочее. Раньше сюда не смотрели вовсе — шапка двойника собиралась с нуля,
    # и объявленное молча пропадало. Так английская страница пожертвований
    # потеряла `layout: donate`, а с ним и кнопки копирования крипто-адресов:
    # адреса остались на ней текстом, и скопировать их стало нечем.
    extra = []
    for line in head.split('\n'):
        f = FMLINE.match(line)
        if not f:
            continue
        name, value = f.group(1), f.group(2)
        if name in OURS or name in NEVER:
            continue
        if name in PROSE:
            ru = value.strip().strip('"')
            if not dry and CYR.search(ru):
                got = phrase(ru, cache, stats)
                if got is None:
                    stats['fields'] = stats.get('fields', 0) + 1
                value = '"%s"' % (got or ru).replace('"', "'")
            extra.append('%s: %s' % (name, value))
            continue
        extra.append(line)

    done = []
    for b in blocks(body):
        if not b.strip() or not CYR.search(b) or FENCE.match(b.strip()):
            done.append(b)
            continue
        if dry:
            stats['would'] = stats.get('would', 0) + 1
            done.append(b)
            continue
        got = whole(b, cache, stats)
        # Целым кусок не дался — разбирается на части: то, что лежит между
        # метками, переводится по одному, и метки к модели не едут вовсе.
        # Порядок такой, а не обратный, потому что часть переводится, не видя
        # соседей: пока кусок даётся целиком, он и лучше.
        bad = 'stuck' if got is None else sound(got, b)
        if bad or not framed(got, b):
            # Собранное по частям не сличается с исходником целиком (`answered`)
            # — только на поломки. Мерить его тем же правилом было бы неверно:
            # оно требует, чтобы русского почти не осталось, а здесь остаток —
            # это честно отвергнутая часть, и таких в строфе бывает две из
            # двадцати. Каждая часть уже проверена своей меркой (`fits`), а
            # хуже русского собранное не бывает: непереведённая часть остаётся
            # ровно тем, чем была.
            piece = piecemeal(b, cache, stats)
            if (piece is not None and piece != b and not sound(piece, b)
                    and framed(piece, b)):
                stats['piece'] = stats.get('piece', 0) + 1
                got, bad = piece, None
        if bad:
            stats[bad] = stats.get(bad, 0) + 1
            got = None
        elif not framed(got, b):
            # Разметка съедена, а текст переведён. Это хуже безупречного
            # перевода и лучше русского абзаца: заголовок, ставший абзацем,
            # читается, непрочитанный — нет.
            stats['frame'] = stats.get('frame', 0) + 1
        done.append(got if got is not None else b)
    # Подписи внутри тегов — последними и по всей странице: они стоят и в
    # кусках, где переводить больше нечего, а до модели тег не доходит.
    body = '\n\n'.join(done)
    return en_title, extra, body if dry else relabel(body, cache, stats)


def write(rel, en_title, extra, body, have):
    dest = os.path.join(OUT, rel)
    os.makedirs(os.path.dirname(dest), exist_ok=True)
    head = '---\n'
    if en_title:
        head += 'title: "%s"\n' % en_title.replace('"', "'")
    # `lang` — не только для <html lang>. Его же читает поисковый указатель:
    # страница, назвавшая язык, попадает в выдачу только тому, кто читает на
    # этом языке. Пока движок языков не знал, перевод из поиска исключался
    # целиком (`search: false`), иначе русскому читателю каждая находка
    # выдавалась дважды, на двух языках. Теперь исключать нечего: обе выдачи
    # одноязычны, и перевод наконец ищется — на своих страницах.
    head += 'lang: en\n'
    # Обратный путь. Считается тем же `twin()`, что и путь туда, — своя
    # арифметика здесь однажды уже соврала: `index.md` она обращала в
    # «/index/», и переключатель с английской главной вёл в 404. Адрес
    # страницы и адрес её двойника — одно знание, и живёт оно в одном месте.
    head += 'ru: %s\n' % twin(rel)[len('/en'):]
    # Своё у страницы — следом, чтобы сверху стояло сказанное переводом, а под
    # ним перенесённое как есть.
    for line in extra:
        head += line + '\n'
    head += '---\n\n'
    open(dest, 'w', encoding='utf-8').write(head + relink(body, have).rstrip() + '\n')


# Что стоит на английской странице вместо перевода. Текста два, потому что
# происхождение русского разное, и читателю эта разница важна: в одном случае
# русское здесь — работа Габриэля, в другом — наша работа по его английскому.
WHOSE = {
    'his': ("The Russian on this page is **Gabriel Pradīpaka's own translation**, kept "
            "here unchanged so that a stanza can be found by any word in it. Rather than "
            "run his work through a machine and back into English, here is his own English:"),
    'here': ("The Russian on this page was made **for this site, out of Gabriel Pradīpaka's "
             "English**. Translating it back would hand you a third-hand retelling of a text "
             "whose original is one link away:"),
}


def stub(rel, en_title):
    """Английская страница взамен непереведённой: чей текст и куда за подлинником.

    Ссылка ведёт **на эту же часть**, а не на раздел вообще: она взята с самой
    страницы (`source()`), и заглавная раздела остаётся лишь на случай, когда
    ссылки на странице почему-то нет.
    """
    pref, name, home, whose = section(rel)
    text = open(os.path.join(ROOT, rel), encoding='utf-8').read()
    src = source(text) or home
    ru_url = twin(rel)[len('/en'):]
    en_home = '/en/' + pref
    head = ('---\n'
            'title: "%s"\n'
            'lang: en\n'
            # Своего текста у такой страницы нет — искать в ней нечего.
            'search: false\n'
            'source: %s\n'
            'ru: %s\n'
            '---\n\n') % ((en_title or name).replace('"', "'"), src, ru_url)
    body = (
        '<p class="pv-crumbs nosearch" markdown="1">[Kashmir Shaivism](/en/ksh/) · '
        '[%(name)s](%(home)s) · [Glossary](%(home)sglossary/)</p>\n\n'
        '# %(title)s\n\n'
        '> **Not translated here, and on purpose.** %(whose)s\n'
        '>\n'
        '> **[Read this part in the author\'s own English](%(src)s)**\n'
        '>\n'
        '> The Russian page you were heading for is [still here](%(ru)s) — the Sanskrit '
        'on it is the same Sanskrit.\n'
    ) % {'name': name, 'home': en_home, 'title': en_title or name,
         'whose': WHOSE[whose], 'src': src, 'ru': ru_url}
    dest = os.path.join(OUT, rel)
    os.makedirs(os.path.dirname(dest), exist_ok=True)
    open(dest, 'w', encoding='utf-8').write(head + body)


def roster(todo):
    """Список переведённых страниц — в `_data/i18n.yml`, для макета.

    Макету надо знать две вещи: есть ли у русской страницы английский двойник
    (тогда на ней стоит переключатель и `hreflang`) и наоборот. У английской
    страницы обратный адрес лежит во front matter (`ru:`), а русская о своём
    двойнике сама знать не может — оттуда и список.
    """
    path = os.path.join(ROOT, '_data', 'i18n.yml')
    os.makedirs(os.path.dirname(path), exist_ok=True)
    # Двойник считается по файлу, а не по намерению: страница, которую в этот
    # заход не писали, попадает в список, только если её двойник уже лежит на
    # месте с прошлого раза. Иначе список пообещал бы переключатель туда, где
    # переключаться не на что.
    made = [rel for rel in todo if os.path.exists(os.path.join(OUT, rel))]
    urls = sorted(set(twin(rel)[len('/en'):] for rel in made) | set(BYHAND))
    with open(path, 'w', encoding='utf-8') as f:
        f.write('# Собирается `tools/translate.py`. Руками не править.\n')
        f.write('#\n# Адреса русских страниц, у которых есть английский двойник в /en/.\n')
        f.write('en:\n')
        for u in urls:
            f.write('  - "%s"\n' % u)
    return len(urls)


def main():
    ap = argparse.ArgumentParser(description=__doc__)
    ap.add_argument('only', nargs='*', help='только эти страницы')
    ap.add_argument('--list', action='store_true', help='что пойдёт в перевод')
    ap.add_argument('--dry', action='store_true', help='прогнать, ничего не покупая')
    ap.add_argument('--workers', type=int, default=8, help='сколько запросов разом')
    args = ap.parse_args()

    rows = pages()
    if args.list:
        for kind, what in (('ours', 'переводим'), ('source', 'ссылка на подлинник'),
                           ('no', 'не трогаем')):
            part = [r for r in rows if r[1] == kind]
            print('%s: %d' % (what, len(part)))
            if kind != 'ours':
                for rel, _k, why in part:
                    print('   %-40s %s' % (rel, why))
        return 0

    todo = [r[0] for r in rows if r[1] == 'ours']
    refs = [r[0] for r in rows if r[1] == 'source']
    # Двойник есть и у страницы-заместителя: иначе ссылка на главу с английской
    # заглавной раздела вела бы прямо в русский текст, ничем не предупредив.
    #
    # Считается это по всему сайту, и считается **до** того, как список сузили
    # до заказанных страниц. Английское дерево от того, что переводят одну
    # главу, не уменьшается: сузь состав здесь — и ссылки на прочие главы
    # уедут в русский текст, а `_data/i18n.yml` объявит, что двойник есть у
    # четырёх страниц из ста девяноста одной, и на остальных ста восьмидесяти
    # семи пропадёт переключатель языка. Проверено дорого: прогон одной главы
    # ровно это и сделал (VS-47).
    whole = todo + refs
    have = {twin(r)[len('/en'):] for r in whole} | set(BYHAND)

    if args.only:
        todo = [r for r in todo if r in args.only]
        refs = [r for r in refs if r in args.only]
        if not todo and not refs:
            sys.exit('таких страниц в переводе нет: %s' % ' '.join(args.only))

    cache, stats = Cache(CODE), {}

    if not args.dry:
        heads, bodies = [], []
        for rel in todo:
            top, out = wanted(rel)
            heads += top
            bodies += out
        heads += [t for t in (title_of(r) for r in refs) if t]
        print('кусков %d, покупаем недостающие…' % (len(heads) + len(bodies)), flush=True)
        print('куплено: %d' % warm(heads + bodies, cache, args.workers))
        # Второй заход — за то, на что ответили не переводом. Он здесь, а не в
        # сборке, по той же причине, по какой здесь первый: восемьдесят запросов
        # подряд это восемьдесят дорог до сервера, а разом — одна.
        # Заходы со второго по третий: за то, на что ответили не переводом.
        # Наказа два, и второй спрашивается только у тех, кому не помог первый.
        for _ in tries(''):
            again = waiting(bodies, cache)
            if not again:
                break
            n = sum(len(v) for v in again.values())
            print('переводом не ответили: %d — переспрашиваем' % n, flush=True)
            for kind, part in sorted(again.items()):
                print('куплено переспросом (%s): %d'
                      % (kind, warm(part, cache, args.workers, kind=kind)))
        # Третий заход — за частями тех кусков, что не дались и с переспроса, и
        # за подписями внутри тегов. И то и другое — короткие куски одного
        # рода, и наказ у них один.
        small = hopeless(bodies, cache)
        for rel in todo:
            small += labels(open(os.path.join(ROOT, rel), encoding='utf-8').read())
        if small:
            print('кусок целиком не дался: частей и подписей %d' % len(small), flush=True)
            print('куплено по частям: %d'
                  % warm(small, cache, args.workers, kind='bit'))

    for n, rel in enumerate(todo, 1):
        en_title, extra, body = do(rel, cache, stats, args.dry)
        if not args.dry:
            write(rel, en_title, extra, body, have)
        if n % 20 == 0 or n == len(todo):
            print('%3d/%d  %s' % (n, len(todo), stats), flush=True)

    for rel in refs:
        ru_title = title_of(rel)
        en_title = ru_title
        if ru_title and not args.dry:
            # Заголовок у этих страниц — номер и имя: «глава 9», «афоризм 3»,
            # «строфы 3–4, часть 1». Номер тут не украшение: по нему читатель
            # сверяет, туда ли попал, и по нему же страницы выстраиваются в
            # палитре перехода. Пропал или переменился — заголовок остаётся
            # русским: русский заголовок хуже читается, неверный номер врёт.
            # Правило это теперь общее с переведёнными страницами и живёт в
            # `looks()`: разбор у заголовка один, на какой бы странице он ни
            # стоял.
            got = phrase(ru_title, cache, stats)
            if got:
                en_title = got
            else:
                stats['titles'] = stats.get('titles', 0) + 1
        if not args.dry:
            stub(rel, en_title)
    if refs:
        print('со ссылкой на подлинник: %d' % len(refs))
    cache.save()
    if not args.dry:
        print('в _data/i18n.yml: %d страниц' % roster(whole))
    print('готово: %s' % stats)
    if stats.get('lost'):
        print('вернулись без меток и остались по-русски: %d' % stats['lost'])
    if stats.get('marks'):
        print('испортили бы подстрочник и остались по-русски: %d' % stats['marks'])
    if stats.get('mixed'):
        print('дали слово из двух алфавитов и остались по-русски: %d' % stats['mixed'])
    if stats.get('bold'):
        print('потеряли звёздочку и остались по-русски: %d' % stats['bold'])
    if stats.get('again'):
        print('ответили не переводом и были переспрошены: %d' % stats['again'])
    if stats.get('piece'):
        print('не дались целиком и собраны по частям: %d' % stats['piece'])
    if stats.get('frame'):
        print('перевод съел разметку строки: %d' % stats['frame'])
    if stats.get('bits'):
        print('частей не далось и осталось по-русски: %d' % stats['bits'])
    if stats.get('labels'):
        print('подписей внутри тегов осталось по-русски: %d' % stats['labels'])
    if stats.get('stuck'):
        print('не дались ни целиком, ни по частям: %d' % stats['stuck'])
    if stats.get('titles'):
        print('заголовок не дался и остался русским: %d' % stats['titles'])
    if stats.get('fields'):
        print('поле front matter не далось и осталось русским: %d' % stats['fields'])
    if stats.get('reask'):
        print('переспрошено заголовком: %d' % stats['reask'])
    return 0


if __name__ == '__main__':
    sys.exit(main())
