remote_theme: "dracula/gh-pages"
google_analytics: "G-Z1L94CNEJN"

# Язык сайта. Без него макет темы объявлял каждую страницу английской
# (`lang="en-US"` — её умолчание), а сайт русский: читалка с экрана бралась
# читать русский текст по-английски, а поисковик считал его английским.
# Страница на другом языке ставит себе `lang:` во front matter и перебивает.
lang: ru

# Макет по умолчанию — свой (`_layouts/default.html`), а не темы: у макета темы
# нет ни одной точки расширения, и скрипт, который должен работать на каждой
# странице, вставить было некуда. Подробности — в самом макете.
#
# Раньше страницы получали макет темы, не называя его нигде, и как именно —
# понять по конфигу было нельзя. Теперь сказано прямо. Страницы, которым макет
# не нужен, ставят себе `layout: null` (указатели), а `donate` называет свой.
defaults:
  - scope:
      path: ""
    values:
      layout: default

  # Адрес страницы — тот, каким её зовут ссылки: без «.html».
  #
  # Сайт почти весь состоит из каталогов: 313 страниц из 399 лежат в
  # `index.md`, и адрес у них и так `/ksh/`. Остальные 79 лежат файлами, и
  # `page.url` отдаёт у них имя файла — `/dance/ns-ch5.html`. Ссылки же сайта
  # пишутся без расширения, все 228 до одной. А из `page.url` строятся
  # `canonical`, `og:url` и адреса в трёх поисковых указателях — и все они
  # называли страницу не так, как зовёт её сайт (VS-52). Тем же расхождением
  # молчал переключатель языка (VS-49) и не узнавался двойник (VS-46).
  #
  # Постоянная ссылка без `:output_ext` снимает расширение с адреса, а файл
  # оставляет на месте: к пути назначения Jekyll дописывает `.html` сам, если
  # там его нет. Поэтому не переезжает ни один файл, не переписывается ни одна
  # ссылка, и прежний адрес с `.html` продолжает отдаваться — уже как
  # неканонический синоним, а не как то, что страница объявляет о себе.
  #
  # Второй путь, `permalink: pretty`, дал бы третий вид адреса —
  # `/dance/ns-ch5/`: файлы переехали бы в каталоги, прежние `.html` стали бы
  # отдавать 404, а все 228 ссылок — редирект.
  #
  # Страницы-файлы есть только тут, в `dance/` и `en/dance/`; правило глядит
  # на каталог целиком, так что новая глава получит такой адрес сама. За
  # `*.md` оно не выходит: `search-index.json` в `dance/` — тоже страница, и
  # свой адрес называет сам.
  - scope:
      path: "dance/*.md"
    values:
      permalink: "/:path/:basename"
  - scope:
      path: "en/dance/*.md"
    values:
      permalink: "/:path/:basename"
  # Указатель каталога — это каталог: `/dance/`, а не `/dance/index`. Правило
  # выше глядит и на него, поэтому эти двое названы отдельно: путь длиннее —
  # значит перебивает, так Jekyll выбирает между совпавшими правилами.
  - scope:
      path: "dance/index.md"
    values:
      permalink: "/dance/"
  - scope:
      path: "en/dance/index.md"
    values:
      permalink: "/en/dance/"

# `{% seo %}` в макете. Тема объявляет его у себя, но на своё «plugins»
# полагаться нельзя: локальная сборка идёт без темы.
plugins:
  - jekyll-seo-tag

# tools/ — конвейеры сборки /ksh/pv/, /ksh/ta/ и /ksh/tantrasara/, а не содержимое сайта.
# Список повторяет умолчания Jekyll: своё «exclude» их заменяет, а не дополняет.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/
  - tools/
  # Указания агенту — не содержание сайта, и страницей быть не должны: файл
  # уезжал на сайт и попадал в палитру перехода. `search: false` у него стоял,
  # но это про поиск, а палитра ищет по названиям и своего указателя.
  #
  # Перевода у него больше нет вовсе — конвейер обходил все .md подряд и делал
  # английскую страницу из этого файла, как из любой другой; теперь он в SKIP
  # (tools/translate.py), и исключать переведённое отдельно не приходится.
  - CLAUDE.md
  # Из общего репозитория поиска сайту нужны search.js, palette.js и сборщик
  # указателя (он сам просится в корень своим permalink). Проверки оттуда —
  # его собственные, они сличают два его файла между собой и на сайте не
  # нужны; уехав, они лежали бы там на виду и без дела.
  - sitesearch/README.md
  - sitesearch/FORMAT.md
  - sitesearch/check-fold.js
  - sitesearch/check-prepare.js
  - sitesearch/node/
